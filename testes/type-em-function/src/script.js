"use strict";
function firstLetterUpperCase(name4) {
    let firstLetter = name4.charAt(0).toUpperCase;
    return $;
    {
        firstLetter;
    }
    +name4.substring(1);
}
// também pode usar um type, ao invés de declarar direto na função
